# AGENT RULES — STRICT MODE (ОБОВ'ЯЗКОВО ДО ВИКОНАННЯ)

Цей файл є головним джерелом правил для будь-яких змін у проєкті.
ПЕРЕД БУДЬ-ЯКОЮ ДІЄЮ агент ЗОБОВ’ЯЗАНИЙ прочитати цей файл.

Порушення будь-якого пункту = неправильна реалізація.

--------------------------------------------------
1. ГОЛОВНА ЦІЛЬ
--------------------------------------------------

Сайт має бути максимально точною копією https://yoursite.com/

НЕ допускається:
- “приблизно так”
- “схоже”
- “я трохи покращив”
- “я зробив по-своєму”

ДОПУСКАЄТЬСЯ ТІЛЬКИ:
- піксель-перфект реалізація
- точне повторення структури
- точне повторення поведінки

--------------------------------------------------
2. ДИЗАЙН І ВЕРСТКА
--------------------------------------------------

ЗАБОРОНЕНО:
- змінювати відступи
- змінювати шрифти
- змінювати кольори
- змінювати розміри елементів
- спрощувати блоки
- видаляти частини UI

ДОЗВОЛЕНО:
- тільки точне відтворення

Якщо є сумнів — ПОВТОРИТИ ЯК В ОРИГІНАЛІ.

--------------------------------------------------
3. АРХІТЕКТУРА
--------------------------------------------------

ОБОВ’ЯЗКОВО:
- компонентний підхід
- розділення логіки і представлення
- повторювані блоки → components/

ЗАБОРОНЕНО:
- дублювати код
- писати все в одному файлі
- змішувати HTML + логіку без потреби

--------------------------------------------------
4. МОВИ (CRITICAL)
--------------------------------------------------

Структура URL:

- Польська (default): /
- Українська: /uk/
- Російська: /ru/
- Англійська: /en/

ОБОВ’ЯЗКОВО:
- всі тексти через lang/*.php
- жодного хардкоду текстів у шаблонах
- повна синхронізація між мовами

hreflang:
- pl-PL (default)
- uk-UA
- ru-RU
- en-US
- x-default → польська

--------------------------------------------------
5. ФОРМА (CRITICAL)
--------------------------------------------------

ЗАБОРОНЕНО:
- використовувати mail()

ОБОВ’ЯЗКОВО:
- PHPMailer
- SMTP (через config або .env)

БЕЗПЕКА:
- server-side validation
- sanitize input
- перевірка email
- honeypot поле
- базовий rate limit

UX:
- без перезавантаження сторінки (AJAX)
- повідомлення мовою користувача

--------------------------------------------------
6. КОНФІГУРАЦІЯ
--------------------------------------------------

Всі змінні винести:

- email отримувача
- SMTP налаштування
- базові параметри

Файл:
- config.php або .env

ЗАБОРОНЕНО:
- хардкодити значення у коді

--------------------------------------------------
7. SEO (CRITICAL)
--------------------------------------------------

ОБОВ’ЯЗКОВО:

- 1 H1 на сторінку
- правильна ієрархія H1–H6
- alt для ВСІХ зображень
- унікальні title/description для кожної мови

ТЕХНІЧНО:
- sitemap.xml
- robots.txt
- canonical
- hreflang
- Open Graph

ПРОДУКТИВНІСТЬ:
- lazy loading
- preload hero
- оптимізація зображень

--------------------------------------------------
8. АДАПТИВНІСТЬ
--------------------------------------------------

ОБОВ’ЯЗКОВО протестувати:

- 1920px
- 1440px
- 1024px
- 768px
- 480px
- 390px
- 360px

ЗАБОРОНЕНО:
- ламати верстку
- “і так норм на мобілці”

--------------------------------------------------
9. КОД
--------------------------------------------------

ОБОВ’ЯЗКОВО:
- чистий код
- зрозумілі назви змінних
- логічна структура

ЗАБОРОНЕНО:
- костилі
- зайві бібліотеки
- “тимчасові рішення”

--------------------------------------------------
10. НЕ ЛАМАТИ ІСНУЮЧЕ
--------------------------------------------------

Перед будь-якою зміною:

1. Перевірити що вже працює
2. Не зламати існуючий функціонал
3. Не видаляти нічого без причини

--------------------------------------------------
11. ПЕРЕД ЗАВЕРШЕННЯМ
--------------------------------------------------

ОБОВ’ЯЗКОВО перевірити:

- всі сторінки працюють
- всі мови працюють
- перемикач мов працює
- форма відправляє лист
- немає JS помилок
- адаптив не ламається
- SEO базово коректний

--------------------------------------------------
12. ЗАБОРОНЕНО ІГНОРУВАТИ ЦЕЙ ФАЙЛ
--------------------------------------------------

Перед кожною задачею:

1. Прочитати цей файл
2. Дотримуватись ВСІХ правил
3. Не вигадувати нові підходи без причини

Якщо є сумнів — обрати варіант максимально близький до оригіналу.

--------------------------------------------------
13. СИСТЕМА КЕРУВАННЯ КОНТЕНТОМ (CMS) ТА АДМІН-ПАНЕЛЬ
--------------------------------------------------

Всі ключові текстові блоки та прайс-лист керуються через базу даних та адмін-панель (`/admin`).

ОБОВ’ЯЗКОВО:
- Усі нові статичні тексти на фронтенді додавати через функцію `tr('ключ_тексту', 'Текст за замовчуванням')` та реєструвати у таблиці `content_blocks`.
- Структура прайс-листа є трирівневою: `service_categories` (вкладки) -> `service_subcategories` (блоки послуг) -> `service_items` (конкретні послуги з цінами).
- При зміні структури чи додаванні нових послуг, зберігати можливість їх редагування/видалення через інтерфейс `admin/services.php`.

ЗАБОРОНЕНО:
- Хардкодити нові послуги або ціни прямо в HTML (`pages/home.php`). Всі дані прайс-листа повинні тягнутися з бази даних.
- Вносити зміни напряму в базу даних для редагування контенту, якщо для цього вже передбачено інтерфейс в адмін-панелі.
